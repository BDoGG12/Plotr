import Foundation

enum Constants {
    /// RevenueCat public SDK key for the iOS app.
    /// Replace this placeholder with the real key from the RevenueCat dashboard
    /// (Project settings → API keys → public app-specific key, prefixed with `appl_`).
    static let revenueCatAPIKey: String = "appl_IjZxTZNzlgPjEbTiAOTfHsASFsh"
}
